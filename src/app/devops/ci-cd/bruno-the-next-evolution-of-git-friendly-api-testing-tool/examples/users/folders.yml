## users/folders.yml
# Folder-level variables applied to all requests in `users/`
base_path: /users
default_role: tester
folder_auth_token: folder-specific-token
timeout_ms: 5000

# Example of nested config (optional)
rate_limit:
  requests: 100
  per_seconds: 60
